type Query {
  hello(name: String): String!

  getCurrentUser(accessToken: String): User
}

type Mutation {

}

type User {
  username: String
  email: String
  thumbUrl: String
  accessToken: String
  idToken: String
  expiresAt: String
}
