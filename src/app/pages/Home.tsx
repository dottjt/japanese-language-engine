import * as React from 'react';
import { Flex } from 'rebass';

class Home extends React.PureComponent<{}, {}> {
  public render() {
    return (
      <Flex>
        home
      </Flex> 
    );
  };
};

export default Home;
