import * as React from 'react';
import { Flex } from 'rebass';

class Prerequistes extends React.PureComponent<{}, {}> {
  public render() {
    return (
      <Flex>
        prerequistes
      </Flex> 
    );
  };
};

export default Prerequistes;
